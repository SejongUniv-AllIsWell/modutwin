"""
방 경계면 바깥의 floater GS 입자들을 가장 가까운 경계면에 투영한다.

흐름:
    GS 입자 위치 + boundary.py에서 추출한 6개 평면
        ↓
    각 입자가 bounding box 바깥인지 판별
        ↓
    바깥 입자 → 가장 가까운 경계면에 수직 투영 (위치 이동)
    투영된 입자의 공분산(모양/방향)도 해당 벽면과 나란하게 정렬
        ↓
    수정된 GS 입자들을 원본에 반영

TODO:
- 6개 평면 기준 bounding box 내/외부 판별 구현
- 수직 투영 구현 (위치)
- GS 공분산 행렬을 벽면 방향에 맞게 flatten 처리
- 입력/출력 포맷 결정 (ply 직접 수정 vs 인덱스+오프셋 반환)
"""
