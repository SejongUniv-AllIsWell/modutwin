// Retained for SOG export: k-means (SH palette) + 1D quantization (scales/colors).
export { kmeans } from './k-means';
export { KdTree } from './kd-tree';
export type { KdTreeNode } from './kd-tree';
export { quantize1d } from './quantize-1d';
